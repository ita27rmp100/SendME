<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="icon" href="/images/SendMe_logo.png" type="image/png">
  </head>
  <body class="lead">
    <%- include('./master/navbar.ejs') %>
    <nav class="container-fluid navHomePAge">
      <div class="row">
        <!-- greeting / getting the link to receive messages -->
        <div class="col">
          <h1>SendMe</h1>
          <p class="text-muted">
            Welcome <b><%= username %></b> to our website.
              <br>
            This website is to share secret messages with your close freinds .
          </p>
          <div class="input-group mb-3 flex-column">
            <div class="input-group-prepend">
              <span class="input-group-text" id="basic-addon1">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-back" viewBox="0 0 16 16">
                  <path d="M0 2a2 2 0 0 1 2-2h8a2 2 0 0 1 2 2v2h2a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2v-2H2a2 2 0 0 1-2-2V2zm2-1a1 1 0 0 0-1 1v8a1 1 0 0 0 1 1h8a1 1 0 0 0 1-1V2a1 1 0 0 0-1-1H2z"/>
                </svg>
              </span>
              <input id="link" type="text" class="form-control" placeholder="Get your link" aria-label="Username" aria-describedby="basic-addon1" value="send/<%=username %>" data-toggle="tooltip" data-placement="bottom" data-original-title="Share your link with the others ." readonly>
            </div>
            <form action="/" method="post">
              <input type="submit" value="Log out" id="logOut" class="btn btn-outline-dark float-right right m-5 w-25 align-self-end">
            </form>
          </div>
        </div>
        <!-- This div is to to show what poeple have sent to you -->
        <div class="col">
          <table class="table table-bordered table-striped table-hover">
            <thead>
              <tr>
                <th>from :</th>
                <th>message :</th>
              </tr>
            </thead>
            <tbody>
              
            </tbody>
          </table>
        </div>
      </div>
    </nav>
  </body>
  <%- include('./master/scripts') %>
  <script>
    $(document).ready(
      function() {
        $('#link').click(
          function(){
            window.location = $(this).val()
          }
        )
        $('tbody').html(decodeURIComponent('<%- escape(MT) %>'))
      }
    )
  </script>
</html>
